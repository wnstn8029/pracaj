<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.EmpDao">
 
<!--  <insert id="insertEmp" parameterType="dto.EmpDto">
    insert into emp(empno,
                      ename,
                      job,
                      mgr,
                      hiredate,
                      sal,
                      comm,
                      deptno)
      values(        
             #{empno},
             #{ename},
             #{job},
             #{mgr},
             #{hiredate},
            #{sal},
            #{comm},
            #{deptno}
            )
 </insert>
 updateEmp(유저수정하기) 
 <update id="updateEmp" parameterType="dto.EmpDto">
  update emp set  ename=#{ename} , job=#{job} ,
                   mgr=#{mgr} ,hiredate=#{hiredate} ,sal=#{sal} ,comm=#{comm} ,deptno=#{deptno}
  where empno=#{empno} 
 </update>
 deleteEmp(글삭제하기)
 <delete id="deleteEmp">
  delete from emp where empno=#{empno}
 </delete> -->
 <!-- getEmpList(전체조회) -->
 <select id="getEmpList" resultType="dto.EmpDto">
       select * from emp      
 </select>



</mapper>